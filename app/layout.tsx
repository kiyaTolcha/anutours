import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Anutu Tours - Discover Amazing Adventures',
  description: 'Experience unforgettable tours and adventures with Anutu Tours. Explore breathtaking destinations with expert guides and premium service.',
  keywords: 'tours, travel, adventure, destinations, guided tours, vacation',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
